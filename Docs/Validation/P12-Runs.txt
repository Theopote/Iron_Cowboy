P12 validation — 2026-09-13 to 2026-09-14 — Unreal Engine 5.8.2

Build:
  .\Scripts\Build.ps1 -EngineRoot 'C:\Program Files\Epic Games\UE_5.8'
  Result: SteppeEditor Win64 Development succeeded.

Automation:
  .\Scripts\RunEditor.ps1 -Tests -ExpectedTests 17 -Commands 'Automation RunTests Steppe' -LogName P12-FinalAutomation
  Result: 17 passed, 0 failed, 2 tests with the existing RiderSeat placeholder warning.

Lasso skill render smoke:
  .\Scripts\RunEditor.ps1 -Game -Render -Smoke -LassoSkillSmoke -Commands '' -LogName P12-LassoSkillRender
  Result: stable window rendered; Stability=1.00, Zone=Neck, Radius=80.0 cm, Range=2600.0 cm; persistent NECK feedback rendered after attachment.

Full-loop regression:
  .\Scripts\RunEditor.ps1 -Game -Render -Smoke -FullLoopSmoke -Commands '' -LogName P12-BalanceFullLoop
  Result: horse followed through Movement, entered camp, showed Horse Card and was named Saran; Trial Success.

Balance and dragged render smoke:
  .\Scripts\RunEditor.ps1 -Game -Render -Smoke -BalanceSmoke -Commands '' -LogName P12-BalanceRender
  Result: side load reached Balance 1.00 with Side 1.00, force-dismounted the rider into Dragged while the lasso stayed attached; active LMB release moved both rider balance and lasso into Recovering.
